// this is just the interface for the firetv json, it's here just to help with ts and vscode autocompletion
export interface FTVJson {
  url: string;
  width: number;
  height: number;
  description: string;
}
